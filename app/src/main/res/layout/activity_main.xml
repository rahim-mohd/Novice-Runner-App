<?xml version="1.0" encoding="utf-8"?>
<!--
    ACTIVITY_MAIN.XML - Main Container Activity
    ============================================
    This is the main container layout for the app. It serves as the host for:
    - A custom Toolbar with the app title
    - A FragmentContainerView that displays dynamic content (Zones, Plan, Log, Learn)
    - A BottomNavigationView for switching between tabs

    This is a single-activity, multi-fragment architecture.
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:background="#FFFFFF">

    <!-- Toolbar
    A custom toolbar that replaces the default Action Bar-->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#2196F3"
        android:minHeight="?attr/actionBarSize"
        android:paddingTop="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Toolbar Title - Custom TextView inside the Toolbar -->
        <!-- This replaces the default Action Bar title -->

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Novice Runner App!"
            android:textSize="20sp"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

    </androidx.appcompat.widget.Toolbar>

    <!-- Fragment container for dynamic content
     This is the dynamic content area where fragments are swapped
     The fragment displayed changes based on bottom navigation selection-->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/toolbar"
        app:layout_constraintBottom_toTopOf="@+id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Bottom Navigation
    Bottom navigation bar with 4 tabs: Zones, Plan, Log, Learn -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:elevation="8dp"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/bottom_nav_color"
        app:itemTextColor="@color/bottom_nav_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>